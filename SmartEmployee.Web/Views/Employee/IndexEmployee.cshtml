@model IEnumerable<SmartEmployee.Web.Data.Entities.Employee>

@{
    ViewData["Title"] = "IndexEmployee";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container-fluid">
    <!-- Page Heading -->
    <h1 class="h3 text-gray-800">Employee</h1>
    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <a asp-action="AddEmployee" class="d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm">
                <i class="fas fa-plus fa-sm text-white-50"></i> Create New</a>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered" id="dataTable">
                    <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.employeeDocument)
                        </th> 
                        <th>
                            @Html.DisplayNameFor(model => model.FirstOrDefault().User.FullName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.LaboralInfos.FirstOrDefault().Company.companyName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.LaboralInfos.FirstOrDefault().Position.positionName)
                        </th>
                        <th></th>
                    </tr>
                    </thead>
                    <tbody>
                    @foreach (var item in Model) {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.employeeDocument)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.User.FullName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.LaboralInfos.FirstOrDefault().Company.companyName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.LaboralInfos.FirstOrDefault().Position.positionName)
                            </td>
                            <td>
                                <a asp-action="" asp-route-id="@item.EmmployeeId" class="d-none d-sm-inline-block btn btn-sm btn-warning shadow-sm">
                                    <span class="icon text-black-50">
                                        <i class="fas fa-exclamation-triangle"></i>
                                    </span><span class="text-black-50">Edit</span>
                                </a>
                                <a asp-action="DetailsEmployee" asp-route-id="@item.EmmployeeId" class="d-none d-sm-inline-block btn btn-sm btn-info shadow-sm">
                                    <span class="icon text-black-50">
                                        <i class="fas fa-info"></i>
                                    </span><span class="text-black-50">Details</span>
                                </a>
                                <a asp-action="" asp-route-id="@item.EmmployeeId" class="d-none d-sm-inline-block btn btn-sm btn-danger shadow-sm">
                                    <span class="icon text-white-50">
                                        <i class="fas fa-trash"></i>
                                    </span><span class="text">Delete</span>
                                </a>
                            </td>
                        </tr>
                    }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>